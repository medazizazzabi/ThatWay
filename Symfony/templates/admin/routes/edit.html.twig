{% extends 'admin/admin.html.twig' %}
{% block pagetitle %}
	Routes
{% endblock %}
{% block section %}
	{% form_theme form 'bootstrap_5_layout.html.twig' %}

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<a href="{{path('app_admin_routes')}}" class="btn btn-danger">Cancel</a>
	<button type="submit" class="btn btn-primary">Save</button>
	{{ form_end(form) }}
{% endblock %}
